# Legacy Submit Wrappers

This directory keeps the historical dataset-specific submit wrappers that were used as reconstruction sources while consolidating the public pipeline.

They are archived for reference only.

The primary public interface is now:

- `scripts/submit_stage.sh`
- `scripts/run_stage.sh`
- `configs/datasets/*.sh`
